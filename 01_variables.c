#include<stdio.h>
   int main()
   {
     int i = 10; //Declare and initialize i with 10.
     int j = i; // Gives j the same value as i.
    int a = 2, b = 3, c = 4, d = 5; 
     printf("The value of i is %d \n  and the value of j is %d", i,j); 
     printf("The value of a is %d, b is %d, c is %d and d is %d", a,b,c,d);         
     return 0;
   }