#include<stdio.h>
    int main()
    {

      int a;
     printf("Enter number\n");
      scanf("%d", &a);
      
      printf("The remainder of the division between the given no. and 97 is %d", a%97);
        return 0;
    }